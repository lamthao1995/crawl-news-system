services:
  redis:
    container_name: crawl-redis
    image: redis:${REDIS_VERSION:-7-alpine}
    networks:
      - temporal-network
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - crawl-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 20
      start_period: 5s

  crawl-postgres:
    container_name: crawl-postgres
    image: postgres:${POSTGRESQL_VERSION}
    environment:
      POSTGRES_USER: crawl
      POSTGRES_PASSWORD: crawl
      POSTGRES_DB: crawl
    networks:
      - temporal-network
    ports:
      - "5433:5432"
    volumes:
      - crawl-app-postgres:/var/lib/postgresql/data
      - ./crawl-db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crawl -d crawl"]
      interval: 3s
      timeout: 3s
      retries: 30
      start_period: 10s

  postgresql:
    container_name: temporal-postgresql
    image: postgres:${POSTGRESQL_VERSION}
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
    networks:
      - temporal-network
    # No ports: Temporal uses this internally; not exposed to host by default.
    volumes:
      - temporal-postgres:/var/lib/postgresql/data

  temporal:
    container_name: temporal
    depends_on:
      - postgresql
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PWD=${POSTGRES_PASSWORD}
      - POSTGRES_SEEDS=postgresql
      - DYNAMIC_CONFIG_FILE_PATH=config/dynamicconfig/development-sql.yaml
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_CLI_ADDRESS=temporal:7233
    image: temporalio/auto-setup:${TEMPORAL_VERSION}
    networks:
      - temporal-network
    ports:
      - "7233:7233"
    volumes:
      - ./dynamicconfig:/etc/temporal/config/dynamicconfig

  temporal-ui:
    container_name: temporal-ui
    depends_on:
      - temporal
    environment:
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_CORS_ORIGINS=http://localhost:8080
    image: temporalio/ui:${TEMPORAL_UI_VERSION}
    networks:
      - temporal-network
    ports:
      - "8080:8080"

  temporal-admin-tools:
    container_name: temporal-admin-tools
    depends_on:
      - temporal
    environment:
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_CLI_ADDRESS=temporal:7233
    image: temporalio/admin-tools:${TEMPORAL_ADMINTOOLS_VERSION}
    networks:
      - temporal-network
    stdin_open: true
    tty: true

  crawl-worker:
    build:
      context: ./worker
    depends_on:
      temporal:
        condition: service_started
      redis:
        condition: service_healthy
      crawl-postgres:
        condition: service_healthy
    environment:
      # Avoid gRPC/Netty "Invalid argument" / stuck pollers on some Docker Desktop (IPv6 / native transport).
      JAVA_TOOL_OPTIONS: "-Djava.net.preferIPv4Stack=true -Djava.net.preferIPv6Addresses=false -Dio.grpc.netty.shaded.io.netty.transport.noNative=true"
      TEMPORAL_PEER_HOST: temporal
      TEMPORAL_PEER_PORT: "7233"
      TEMPORAL_NAMESPACE: default
      TEMPORAL_TASK_QUEUE: news-task-queue
      CRAWL_JDBC_URL: jdbc:postgresql://crawl-postgres:5432/crawl
      CRAWL_JDBC_USER: crawl
      CRAWL_JDBC_PASSWORD: crawl
      REDIS_URL: redis://redis:6379/0
      CRAWL_SEED_URL: https://www.investing.com/
      CRAWL_ALLOWED_HOST_SUFFIX: investing.com
      CRAWL_CB_FAILURE_THRESHOLD: "5"
      CRAWL_CB_OPEN_SECONDS: "90"
      # Per-worker concurrency (defaults shown); increase when running multiple workflows in parallel
      WORKER_MAX_ACTIVITIES: "16"
      WORKER_MAX_WORKFLOW_TASKS: "8"
      WORKER_MAX_LOCAL_ACTIVITIES: "16"
      # Gentle per-URL spacing before HTTP fetch; set to "0" to disable.
      CRAWL_PER_URL_SLEEP_MS: "350"
      # Hard cap on per-response body size (bytes) after optional gzip decode.
      CRAWL_MAX_BODY_BYTES: "4194304"
    networks:
      - temporal-network
    restart: on-failure
    # Compose v2 honors deploy.replicas for `docker compose up`. Override on the CLI with
    # `docker compose up -d --scale crawl-worker=N` to change without editing this file.
    deploy:
      replicas: 3

networks:
  temporal-network:
    name: temporal-network

volumes:
  temporal-postgres:
  crawl-app-postgres:
  crawl-redis-data:
